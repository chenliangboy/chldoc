{
  "scripts": {
    "start": "npm run dev",
    "dev": "vitepress dev src",
    "build": "vitepress build src && npm run copy",
    "preview": "vitepress preview src",
    "copy": "build.bat"
  },
  "dependencies": {
    "vitepress": "^1.4.1"
  }
}
